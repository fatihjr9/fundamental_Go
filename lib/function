package main

import "fmt"

func greet(name string) (text string) {
	text = "Halo" + name
	return
}

func add(x, y int) (int, int) {
	return x + y, 0
}

func AddSub(a, b int) (addRes int, subRes int) {
	return a + b, a - b
}

func main() {
	fmt.Println(AddSub(2, 5))
}
